FROM node:22-slim AS base
WORKDIR /app

# Se a instalação do bcrypt falhar (compilação nativa), descomente:
# RUN apt-get update && apt-get install -y python3 make g++ && rm -rf /var/lib/apt/lists/*

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

EXPOSE 5000
CMD ["node", "index.js"]